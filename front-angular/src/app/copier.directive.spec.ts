import { CopierDirective } from './copier.directive';

describe('CopierDirective', () => {
  it('should create an instance', () => {
    const directive = new CopierDirective();
    expect(directive).toBeTruthy();
  });
});
